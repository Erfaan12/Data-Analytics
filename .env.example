# ============================================================
# Tax Data Analyzer – Environment Variables
# Copy to .env and fill in your values:
#   cp .env.example .env
# ============================================================

# Server
PORT=8000
HOST=0.0.0.0
LOG_LEVEL=info          # debug | info | warning | error

# Dataset
DATA_FILE=/data/tax_data.csv
DATA_ROWS=500            # rows to generate when no CSV is present
DATA_SEED=42             # RNG seed for reproducible sample data

# CORS – restrict in production
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=*
